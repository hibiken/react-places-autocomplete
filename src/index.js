import PlacesAutocomplete from './PlacesAutocomplete';
import {
  geocodeByAddress,
  geocodeByPlaceId,
  geocodeByLocation,
  getLatLng,
} from './utils';

export { geocodeByAddress, geocodeByPlaceId, geocodeByLocation, getLatLng };

export default PlacesAutocomplete;
